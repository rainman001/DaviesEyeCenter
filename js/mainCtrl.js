angular.module("daviesEye").controller("mainCtrl", function($scope, $location) {
	
// Handle navbar menu clicks here, if necessary, by using $location to change the hash state to reflect what was clicked. It may work to just handle them in the link href attribute (see nbaRoutes, index.html)

})